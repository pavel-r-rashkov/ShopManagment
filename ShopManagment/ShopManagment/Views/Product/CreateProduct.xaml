<Window x:Class="ShopManagment.Views.Product.CreateProduct"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CreateProduct" Height="300" Width="300"
        DataContext="{Binding CreateProductViewModel, Source={StaticResource ViewModelLocator}}">
    <Window.Resources>
        
    </Window.Resources>
   
    <StackPanel>
        <TextBox Name="nameTextBox" Text="{Binding ProductName, ValidatesOnDataErrors=True, NotifyOnValidationError=true, UpdateSourceTrigger=PropertyChanged}" />
        <TextBlock Text="{Binding ElementName=nameTextBox, Path=(Validation.Errors)[0].ErrorContent}" />
        <TextBox Name="priceBox" Text="{Binding Price, ValidatesOnDataErrors=True, NotifyOnValidationError=true, UpdateSourceTrigger=PropertyChanged}" />
        <TextBlock Text="{Binding ElementName=priceBox, Path=(Validation.Errors)[0].ErrorContent}" />
        <Button Command="{Binding CreateProductCommand}">Add</Button>
        <TextBlock Text="{Binding Message, UpdateSourceTrigger=PropertyChanged}"/>
    </StackPanel>
</Window>
